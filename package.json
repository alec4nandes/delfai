{
    "name": "delfai",
    "version": "1.0.0",
    "description": "",
    "private": true,
    "scripts": {
        "test": "echo \"Error: no test specified\" && exit 1",
        "serve": "firebase serve --only hosting,functions",
        "build": "rmdir /s /q public && cd delfai-react && npm run build && cd ..",
        "start": "cd delfai-react && npm start && cd .."
    },
    "author": "",
    "license": "ISC",
    "dependencies": {
        "firebase": "^10.3.0"
    }
}
